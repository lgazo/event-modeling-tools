# Backlog

* [ ] 001: (mermaid) `agent-workflow-from-discord` fails when rendered with mermaid, but works with evml-tools. Problem identified in the multiline `data` entity without YAML-kinda structure but freeform (which is valid)
* [ ] 002: (mermaid) multiline block rendering broken, everything in one line 
* [ ] 003: (evml+mermaid) configurable rendering of GWTs (pure svg vs svg with html -> expandable blocks)
* [ ] 004: (mermaid) port GWTs from evml
* [ ] 005: quoted identifiers?
* [x] 006: verify: if multiple events in same namespace -> namespace duplicated instead of being one swimlane. "The layout package has a bug — calculateSwimlaneProps never sets swimlane.namespace, so findSwimlaneByNamespace always misses and every namespaced event spawns its own lane."

