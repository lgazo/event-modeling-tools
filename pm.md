# Backlog

* [ ] (mermaid) `agent-workflow-from-discord` fails when rendered with mermaid, but works with evml-tools. Problem identified in the multiline `data` entity without YAML-kinda structure but freeform (which is valid)
* [ ] (mermaid) multiline block rendering broken, everything in one line 
* [ ] (evml+mermaid) configurable rendering of GWTs (pure svg vs svg with html -> expandable blocks)
* [ ] (mermaid) port GWTs from evml
* [ ] quoted identifiers?
* [ ] verify: if multiple events in same namespace -> namespace duplicated instead of being one swimlane

